/** How often to poll for active-committee data (all users with a committeeId). */
export const COMMITTEE_POLL_MS = 15_000;

/** How often chairs poll for admin broadcast notifications. */
export const NOTIFICATION_POLL_MS = 45_000;
